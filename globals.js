export type Region = {
  domain: string,
  locale: string,
  siteStr: string,
  microSiteLocation: string
};

export type SplashParams = {
  hmacMethod: string,
  captchaDuplicate: string,
  clientId: string
};